# Netlify is used only for Identity (auth) + Git Gateway — not for hosting.
# Publishing the admin folder lets invite/recovery email links resolve correctly
# at chimerical-speculoos-cc77d5.netlify.app without requiring a full site build.
[build]
  publish = "netlify-cms"
  command = ""
